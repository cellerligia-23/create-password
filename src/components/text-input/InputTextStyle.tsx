import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputer: {
    textAlign: 'center',
    backgroundColor: '#e5bf3c',
    borderWidth: 1,
    color: 'black',
    height: 50,
    padding: 15,
    width: '100%',
    borderRadius: 10,
    marginTop: 30
  }
});
